---
name: mildsunrise/protobuf-inspector
link: https://github.com/mildsunrise/protobuf-inspector
language: Python
topics:
- serialization
- protobuf
- reverse-engineering
description: "\U0001F575\uFE0F Tool to reverse-engineer Protocol Buffers with unknown\
  \ definition"
writeup: ''
readme: type request wire more will order blob blind along are detect No colored display
  inspector py together Tricks status time guess occurred 0 stack feed or program
  specify integers here https definition errors go a binary force 64 The matching
  applicable non just error save run Read longer exit etc docs shown to better field
  detected name default wanting weird like no structure one pass varint high try format
  on at types instead larger sint64 screenshot embedded google If string obviously
  hexdump your parses the contained lots com s integer within Conversely Example their
  It optional accompanying Vw403MI As should useful see developers imgur analysis
  indicating typically names of such stop recognized d analyze reverse After Google
  dependencies defined it float Please wrongly required and get knowing chunks correctly
  has sure zig blobs bytes tricks CONFIG printed is as found was Just where with able
  create if There Parsing data Usage signed root have md reading you finds dump addition
  contents outside zag Error version an engineering nice about value in protocol without
  encoding still those first When stdin uses print when small positive 4 for ll Zig
  full parsed Protobuf URaWqXz mistakes leave buffers i fields something png assume
  both invalid auto chunk So familiar until trace bit defining representation uint32
  does anything packed reason 3 DWG9MGX use 2 that most stopped by specified whether
  but approaching 32 shows parser argument start unaffected be hierarchy some But
  signedness file parsing can message parse unless raw Simple main so every especially
  would not fails want Main protobuf being point extract there note they 1 details
  make zero encoded level my Wire lost
---
