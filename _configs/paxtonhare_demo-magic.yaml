---
name: paxtonhare/demo-magic
link: https://github.com/paxtonhare/demo-magic
language: Shell
topics:
- demo
- automated
- tool
description: A handy shell script that enables you to write repeatable demos in a
  bash environment.
writeup: ''
readme: type magic seconds Functions more will text usage Where are No manually Waits
  allows mode only max app Tricks index example status points demo from ENTER 0 or
  presenting merely disable here newly successful given before a out ls The l presses
  include prints line run to sh t shown runs immediately Can Then default cd gave
  like no w one whatever However stuff You on ln at Magic npm multiple If set oneline
  the node_modules s within exposes Debug It 5 install wait_ Try Simulates actually
  timeout this home useful git proceeding script trying previous Demo enter Create
  pei of pretend add 20 Network For d rename simply user Prints scenes Allows simulates
  defined it functions rely define and has g Help directory Rather PROMPT_TIMEOUT
  repeatable symlinking waiting them Instead h is as unreliable with handy if cached_node_modules
  Useful connections hide need Usage Put have then you later bin present Enters kbd
  typing Features pauses during demos simulated Started w5 in don cat executed application
  option passed Not looks execute Faking into when print modules for doesn decent
  pe network cmd press capture commands Will captures Command until command wait simulate
  pull Disables interactive proceed log through evidence presentation behind amount
  Getting use that js by now enables shell bash often start Print be some file than
  do n can thorough unless p so node _no environment automatically fake would want
  after oblige output Execute decorate options 1 clear e let typed connection debugging
  your my
---
