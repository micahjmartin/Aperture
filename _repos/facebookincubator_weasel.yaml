---
name: facebookincubator/WEASEL
link: https://github.com/facebookincubator/WEASEL
language: Python
topics:
  - dns 
  - beacon
  - linux
description: DNS covert channel implant for Red Teams.
writeup: ''
readme: must is technique eth0 Status Any how called NS needed packets session detection
  get due few version correctly receives CLI not when Has servers interval foothold
  an account Development server Persistence more Recall Non initiates 255 64 can datagram
  Request encrypted clients authenticate keys hostnames produce you nothing Data executed
  libraries RFC Automatic since that low See recommended Acknowledged concatenation
  sockets However less randint use tunnels DHE bytes communications some interactive
  responds We there 4 Examples implementation stay support system interface collisions
  crypto formatted sends secrecy teams 16 Print callback being SANS like 2 exponent
  communicates p favorite Not part Set only access each us Client as against full
  should above FIN them EXEC UUID been preserved the nonce fixing same first truncated
  hostname scheme way fully 128 python3 Here using detect answers requires break raw
  channel state systems domain those start clue a time supports do identified other
  50 streams up so It guidelines myself query README txt At returning CNAME forward
  involves Note 666 As unique public t data under instructions operation Python communication
  UDP noisy reasons This possible On zone from plaintext operate over call stages
  interpreter requirements Group are To message meant IP forensics active in directly
  uptime type 3526 uses amount Initiating needs operator Random simultaneous information
  tell difficult client Each CTR ongoing between 9 Python3 every minimize common exercise
  payload position private interfaces build established urandom high address Architecture
  ID directional execution output issues IPv4 format Usage running Running Execute
  Streams 32 evaded particular re encrypt A make non features Server if vector don
  regular usage bi Stealthy very Initialization aren Blue exploitation Ensure 507
  stage exchange need into All Stream persistence ACK suspect Diffie No functions
  stream Eval shell PING be Limitations run Format TXT or by result arbitrary terminating
  attribution neth1 requests generation have 24 characters who lifetime n hosts for
  longer rude known kept identifying included establish contained fun crafting say
  configurable pip packetized Ephemeral themselves we malware caught purposefully
  process 0 shared real they isn invocation self max manual pass In will Type see
  ensures oneliner dependencies standard specific than domains case often persistent
  preventing robust had 65 new bandwidth meaning cipher own ensuring commands 5 Requirements
  generally art once order Terminate hope AES minified execute root player uniquely
  try C2 key obfuscation small Checking script responders point native featured ensure
  of SYN request byte reliable regaining instead retrying would little contain important
  was which macOS 52 our deprecated bash AAAA decision multi likely RST Get Evading
  etc stored provided size 1 kernel payloads connection their encoded covert baked
  initialize signal lacking initiate noisier response 2KB entire tunneling Hellman
  long lived subdomain host multiple sender Sessions sent queries s priority understand
  side yourself limit alert md IV means task I no until memory Reconnect language
  across code initially initialized obfuscate Protocol beacon statements Meaning command
  Badness Linux therefore network this modulus detections IPv6 Message attackers but
  reader working os implant does where ciphertext name malformed mode 3 per large
  obfuscated nix create bit WEASEL your with 6 While Testing For control seconds You
  and rolling CSPRNG number Beacon about targeted slow already scripts EVAL AKA to
  subdomains single any pre successfully leave total 8 has its it random give last
  DNS adding launch reason fans further record py emulating left on hex fe80 used
  records The Encryption 400 preamble useful bad
---
