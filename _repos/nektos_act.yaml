---
name: nektos/act
link: https://github.com/nektos/act
language: Go
topics:
- github-actions
- devops
- ci
description: "Run your GitHub Actions locally \U0001F680"
writeup: ''
readme: wikipedia incomplete are Local manager look myself change badge stringArray
  json aur disable https bind different env binary clone runner 16 Secrets l just
  warning intentionally Make_ configured However Runner guidelines configuring also
  embedded set the logo Additionally com It list I Image Use unsupported enter Flags
  were choco expected dependencies flags it secret has ref creating if help present
  build supply Clone during either runners gitter having accompanied into Makefile
  Check Here hosted commands pull command utm_source branch act buster directly works
  repo 2 Pull Installation 18GB bash Fast API than do can v3 not en yay v code r following
  options e make locally type request List iA copy Awesome filesystems badges download
  corresponding demo Contributing virtual based consider Gitter releases Any sh always
  Enable cplee variable w like love You on at Overview unit activity Join utm_content
  interactively s quiet P install optional ubuntu see what necessary S making NixOS
  18 For chocolatey reads Task Ask utm_campaign hate Feedback org directory Rather
  master them as issues globally Why any need sudo contents somevalue favorite v2
  providing Commands contributing involved utm_medium reuse slim mysecret provides
  overridden steps below Actions How dryrun use Docker scoop shell pr workflows test
  provide file per JSON reference p applied environment want curl githubusercontent
  output docker secrets nix tests changes Two eventpath my When match head gif Linux
  order will Run finally 04 pull_request alternative brew quickstart time tags using
  maintain The archlinux prompt actrc 11 repeating line managing to Homebrew checkout
  containers images commit Once determines types string kicking Work Checkout Does
  Scoop stretch nektos windows git Build img Act svg Demo of check software _ Runners
  working workflow exists WARNING get all Alternatively custom state push h tools
  tap is nixpkgs local sample path with Configuration doc 228 contribute root CONTRIBUTING
  md you environments With version an Report base value Think uses earlier Every im
  png rather cp 12 contain j Events github b goreportcard chat but replace raw Let
  node managed would In variables Chocolatey container your above verbose read mode
  execution macos 150 example Arch logging watch from Building report or These golang
  before a out provided offers run Card these event name default Known foo W developer
  Issues package dry format Want If actions prepared MODULE_NOT_FOUND deps 2019 their
  this events add 6 Need already used user each defined and g payload loading To Packer
  hub running head_ref buildpack q MY_SECRET base_ref available then off named source
  specific Support in 10 option GitHub when built 4 for must filesystem action Go
  latest job image packages configuration properly simulate Nix Install 15 that another
  PATH by C be reasons cli n elephant every platform Windows 1 supports Default wiki
  Alternative release files
---
