---
name: brannondorsey/whonow
link: https://github.com/brannondorsey/whonow
language: JavaScript
topics:
- dns
- offensive
- malicious-domains
- redteam
description: A "malicious" DNS server for executing DNS Rebinding attacks on the fly
  (public instance running on rebind.network:53)
writeup: ''
readme: Internet random wikipedia great DEFAULT_ANSWER are illegal instance Show up
  https m AAAA again l just over configured Note dynamically also supported brevity
  set the node_modules Additionally com 5 should Don dynamic Use 34 three addresses
  were five rebind localhost d 53 section themselves it own clashing sure queries
  CSV respond Instead starting provider terminal repeat if chained help number 63
  5000times don having arguments still 823230b05826 lookup network keep 192 assume
  csv a06a5856 README textual works 3 beauty 2 Running form bash seprated A whonow
  DNS_rebinding than can once simple IP 8f058b82 false Domains not isn en v RAM mutated
  configure e public This make zero unique type request bound more 5times max alternate
  complex exploiting 0 specify removed next associated free itself soon exit t dick
  always FIFO like whatever 1time You on at npm octet activity s install everyone
  characters answer See 000 pointing Whonow beginning limit define why trouble org
  rule VPS admin malicious as memory creates strings reply subdomain share perform
  have Rebinding may Accepts server really browser surpasses 4c39 been Rebind full
  timestamp 91f7 records 127 behavior DNS use js domains instances between feel test
  reached 9aa2 3times file much message 1fff uuid p 4415 label fly logfile too MAX_RAM_DOMAINS
  panel tests avoid they page will order spin few reused only together ipv4 time forever
  using All PORT coming added Examples The include queried save responses to origin_policy
  Then images Once no increments 168 screenshot purposes string singular policy never
  who Domain_Name_System times names of software educational all custom users powerful
  state h is path with 228 cloud you an version same Subdomains about value definitions
  reset By its info stdout UUIDs png requested Wikipedia Rules 4dfe saved contain
  representation against entire way author b separate What via One but Respond Testing
  managed after other examples there domain your exceed Using verbose usage Now ram
  Unique publicly plural example period Server subdomains logging from program These
  or a keywords out gotchas placed lets run DISCLAIMER rebinding name default cd one
  upper If incremented Currently their this rules previous ip ipv6 responsible Same
  sender add executing Repeat used each UUID and g To 43 Log running Limitations Anatomy
  found Domain available Usage then requests nameserver attacks counter in address
  10 first labels small for must latest stored 253 both saving Each Optional port
  Gotchas length Names log that another by response start be 2times n cli store LOGFILE
  omitted every 9b07bcd7fbd4 15353 accessible origin That 10000000 inadvertently 1
  wiki
---
