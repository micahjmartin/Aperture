---
name: jasonish/evebox
link: https://github.com/jasonish/evebox
language: Rust
topics:
- netsec
- suricata
- nsm
- ips
- security
- ids
description: Web Based Event Viewer (GUI) for Suricata EVE Events in Elastic Search
writeup: ''
readme: are Elastic JavaScript badge json up https different go binary clone installed
  16 consists runs Embedded sending configured Note also which supported embedded
  the com installations It should dynamic restart localhost Database 9200__ basic
  rebuild load it has visit where if serve alerts build And dev don CHANGELOG sent
  screens issue hosted pull command scripting does management gnu 3 inbox most Installation
  datastore A than can do not More en suitable ELK Node Suricata following e This
  make SELKS 5636 max backend based 0 stack evebox 4096 Webpack size search OR machine
  t sh variable try You on at sqlite host suricata contained licenses install Angular
  See Repository release open org master agpl issues as memory Just web any need have
  Possible generate server browser features networks Event provides existing stable
  __http dist use js jasonish Docker trigger Affero refresh p modern environment want
  Change io docker being changes support require will First Linux blob Run newer time
  Unlike yet here using The frontend lower to checkout Then space NODE_OPTIONS documentation
  Rust useful git Evebox of ligher minimal versions satisfied required get all interface
  Existing 4200 tools is likely with http linux Latest md agent you var v12 database
  With version an same heap Filebeat uses CLI Download Development Requirements old
  png Events against github indices greater URL but written alert recompile viewer
  D setup In GPL container readthedocs your CORS Mode mode app Logstash Server from
  RPM Building or repository enable bother a out Nothing run input event default self
  one replacing Issues package instead very If Status EveBox date Example Currently
  html this Search previous events License add 6 already used stamus builds and To
  Log included hub running found loads An available Usage approach eve elasticsearch
  modification requests Or connect Documentation source Features in wish application
  first javascript export for must Go latest reporting image projects development
  configuration setting requirements proxied link port log Inbox that www by exposing
  be SQLite handling building 9200 note while 1 wiki ready Debian files ElasticSearch
---
