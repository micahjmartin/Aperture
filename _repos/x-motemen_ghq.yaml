---
name: x-motemen/ghq
link: https://github.com/x-motemen/ghq
language: Go
topics: []
description: Remote repository management made easy
writeup: ''
readme: src supplied forces gmail sponsors List will text performed GOPATH Linux more
  are exact detect Local usage _project_ AUTHOR look project Be regardless only macOS
  vim coveralls badge example backend brew VARIABLES binaries from DIRECTORY DESCRIPTION
  specify or xbps organize repository gitconfig query https using go eg a Git currently
  clone gets given The conda releases free placed non y remote pushed Japanese to
  shown Only always created We effective Then default updated GHQ_ROOT book variable
  like no one Void whose You update recursively also which instead Creates host supported
  values multiple google If Status actions prevent set x the clones last com s makes
  nothing Example Currently ghq new list 5 install should becomes this see git Build
  See S svg explicitly names of already Accepted ff darcs used user Without Prints
  it _user_ detailed workflow and get Subversion config all 8 directory common To
  depth master searched printed is found as motemen local path primary with silent
  recursive Configuration create if available need verse root careful have urfave
  you may secondary CONFIGURATION With build an files named Clone source specific
  forge VCS in svn value protocol Mercurial Markdown none option shallow first GitHub
  uses Songmu mercurial structures repos SSH for handbook full HANDBOOK image tries
  flag COMMANDS i configuration provides higher pull alias existing below branch link
  relative expanded Releases Coverage contain buy does against github cannot way case
  settings use via STRUCTURES URL that by equals Built under repositories specified
  NAME _host_ u matched hg installing workflows scoop argument bzr test be vcs subversion
  can cli urlmatch Defaults p unless so _repo_ songmu listed environment want cloned
  Runs io ones ENVIRONMENT SYNOPSIS variables r code c Leanpub paths leanpub other
  following there Manage myproj bazaar 1 Windows e This make fossil happen INSTALLATION
  url When locally match
---
