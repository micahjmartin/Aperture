---
name: feexd/vbg
link: https://github.com/feexd/vbg
language: Python
topics:
  - linux
  - redteam
  - ssh
  - reverse-ssh
description: 'Visual Basic GUI: A Tool to Inject Keystrokes on a SSH Client via an
  X11 Forwarded Session'
writeup: ''
readme: possible intercept specific manager manual using window such mouse also tool
  simulate What gif like events running be X11 deliver server_ip daemon client locally
  pressing commands all dollar these will Since remote Rubber Project Wayland On s
  WM payloads you and things keystrokes expect CoolerVoid windows default exposes
  by compromised send features have works dependencies Trusted automatic typed detection
  4 when https exit instructions their environment wmctrl letters them automatically
  binary open inside Issues It SHIFT python not some apply Payloads the inject is
  With py download requires specify vulnerable sending macOS GUI Y now remotely even
  it github distro uses in after from Installation selection typing XQuartz PoC logic
  Most mapped Known as run trust layout Demo does execute operation connection DEMO
  responsible installed com while allow properly shenanigans read keyboard server
  much used use etc seems cannot dangerous expose probably however so made compatible
  people extensions short ForceCommand work machines trusted exactly that necessary
  lot enumerating Server going ve ssh configure RubberDucky Organization a rootstealer
  don ones The sessions usual on option user Detecting added implemented doesn uppercase
  demo would forwarded another characters modifier XTEST sshd_config sign keycodes
  of parsing recent vbg Keyboard Ducky because if given username handling forwarding
  script find setup did Basic c for are write_cmd its VBG detect Despite manually
  key too known Motivation Client directly I do only create Related with nasty SSH
  mode extension to sorts 100 Usage login X session new similar injecting How via
  Projects recording can connecting well being releases most jpg t Visual calling
  payload logging Simply this
---
