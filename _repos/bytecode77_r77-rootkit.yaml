---
name: bytecode77/r77-rootkit
link: https://github.com/bytecode77/r77-rootkit
language: C
topics:
  - windows
  - rootkit
  - redteam
  - offensive
description: Ring 3 Rootkit DLL
writeup: ''
readme: quality low from one process agenda Page developers I 77 blocking return modules
  through point way Setting However cannot gif setting exemplary library access registry
  complete directories will hacking implementation them route it LoadAppInit_DLLs
  https add dll purposes Installation enumerations in that make testing disabled unstable
  choose does using hook by opportunity illegal After exe also user mode attempts
  are keys always sites for Hiding primary function visualization Rootkits Legitimacy
  thread injection installing anti r77 3 services implemented high beta its correctly
  itself PoC successively hidden uses should Future done locked values The loaded
  Binaries elevated API Deinstallation functions not runs method as example must drop
  bytecode77 rootkit used order and software persistence of utilizing app Other processes
  avoid release virus impossible png what primarily explorer out Such ToDo favicon16
  Downloads Rootkit shared quick running enables provides when 6 0 additionally they
  installed is or be executed signed protect subvert requires hides achieved state
  A instance while planned WinAPI 200 use currently entities BIOS registers because
  once installation driver File com Following threads 1 globally 20Rootkit Install
  startup to can rootkits firmware fileicons custom possibly original usually 77_
  routes ring When zip obscuring registered consider images likely work final calls
  GUID other sys downloads like 2 This considered These best commonly value would
  required file whatever prior payload works AppInit_DLLs Before enabling MinHook
  20Binaries a locking persistance So with any educational security hooking logic
  m TEMP multiple administrator delete into drops at Project their on Also Maximum
  applications Items however goals DLL hide 4 this removed well privileges responsible
  misusing detach reputation In hiding require tasks fits files malware block hardware
  under application How every payloads re install after progress prefix reboot HKEY_LOCAL_MACHINE
  guaranteed redirection RequireSignedAppInit_DLLs the taskmanager x86 kernel
---
