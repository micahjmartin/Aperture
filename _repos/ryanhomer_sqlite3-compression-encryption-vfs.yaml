---
name: ryanhomer/sqlite3-compression-encryption-vfs
link: https://github.com/ryanhomer/sqlite3-compression-encryption-vfs
language: C
topics:
- encryption
- vfs
- compression
- sqlite
description: Compression and Encryption Virtual File System for SQLite 3.
writeup: ''
readme: it pairs back 15 project g package shell E Library uncompressed Code Implement
  Custom allows submit Default by specify such used activate Full Newark URI Run trunk
  As sizes indexing at Os only VFS_NAME www3 index unit has Dallas Compile though
  upper during cevfs_mod development doc section support or not SHA1 disable you intended
  would WAL functions activation decompress managed Versions Once no archive Create
  signature roll out Proper opening http NJ name implement Get build 16 Finally currently
  File decrypt them How lib re clang iOS repo param 2 for compressing path to inserted
  Readline operations that It compress recover implements accommodate portable have
  lost Repositories 32 Static zipvfs Here beyond 3 add bottom link these reuse wasted
  Amalgamation SQLITE_ENABLE_CEROD lower Version No convenient also encrypt supported
  cat specifying ZIP configure new both databases Unlike nodes be command own string
  modify conveniently with line See other Free tests implicit sqlite3_activate_cerod
  more xMethods systems CEVFS c using possible less Then macOS information built set
  as reference tarball char Introduction come pull cevfs_example intercept 1 Most
  static herein in files under level we vacuum and compacting updated some on chances
  was mutex System sites Go result compare cevfs_build assumed OS via the hook ZIPVFS
  Tool details Extension x normally install an custom space need github related Not
  You sqlite3 root fork _not_ persistent data _For errors into TCL h libtool system
  Tags download helpful file were easily API from Decompress operating text Combatibility
  sqlite 12 which free compression Downloads Compatible tree CEROD cerod one Zlib
  b functionality block try pass please cgi parameters newly mode contribute wiki
  commit howitworks com see digit unarchived is Fossil cd standard org done are when
  like Wno located ryanhomer Line 0 destination encrypting cevfs KEY https required
  size different community supplied values s encryption CA Alternatively Contributing
  make example Build block_size libraries issue Compression sqlite3PagerClose easy
  all top Xcode www2 write pager create making Works 3cc between key help code up
  changed first Creating preloaded determined hooks _build temporary Select use o
  track your threading included get directory uses implemented changes versions been
  header page referred created Limitations may 11 tab _xFunctions_ Command _ testing
  transparently version Testing declaration geographically C99 Developing interested
  OK Even its this workstation itself max could handled Source SQLite3 Combine For
  This Time gives Loadable interface things open VACUUM The hex Virtual following
  keyword To can By seamlessly Copy 34 CCCryptor yet Fremont const loadext database
  of originally will _If subsequent 10 src VFS Keep Encryption function multi something
  Otherwise compressed www requests UNCOMPRESSED html extension COMPRESSED moved chars
  read allow goal if If combines TX source append than property default Again vfs
  SQLite embed a
---
