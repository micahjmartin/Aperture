---
name: masterzen/winrm
link: https://github.com/masterzen/winrm
language: Go
topics:
  - cli
  - windows
  - library
description: Command-line tool and library for Windows remote command execution in
  Go
writeup: ''
readme: Status __N mercurial depend _required_ subsequently clone to installation
  or one machine _For checking library standards option Golang tunnel us doesn then
  github dependency complex Negotiate some wish Firewall project on 5 winrm winrs
  through according aspx source must VMs 2842230 available it supports issues setting
  internal Azure be Contact go commands any passing svg GOPATH 12 more type For below
  selenic CA Winrm run WinRM only png up masterzen new Library bazaar master supported
  into a just host information test tests tool target itself updatedeps com pure Mercurial
  what have your set This please domain msdn needs Build documentation because now
  section at Command TransportDecorator v prompt prepare from Warning www running
  Make Auth added co steps prepared you effects 2008R2 order config re The get do
  509 version line an MaxMemoryPerShellMB change Getting Knowledge Bugs cli transport
  Transports input work href ll Server struct paste On 1024 format 150MB risk primary
  https authentication y Parameters Go Windows Base HTTPS g by vs these model following
  blog __Run if compile accounts coveralls API of connecting 85 than PowerShell different
  users script means Do example remote travis repository no require installed 2008
  It disable system branch Dial GSSAPI does repos desktop first Building io check
  microsoft command true You usage article various properly typing Basic fast which
  x By Microsoft scenario src basic dependencies see this aa384426 html HTTP and Usage
  memory _Note_ using in subject If machines service Developing vanilla are When changes
  send directly anymore gokogiri hotfix B http refer as Gokogiri splitted Next Started
  2011 Richard support nor Please might additional including where 7 need next en
  execute Administrator__ has allow but Kerberos DevCenter moovweb looking executable
  2004640 golang lines online windows with call getting canonical possible can not
  install bug automatically pluggable how overthere dialer functions been local r
  Coverage sure make badge Bazaar SSH described s kb Downer org doc EC2 AllowUnencrypted
  WinRS frontiertown latest use ci systems java control e natively Preparing is cert
  without NTLM 1 build __ See _ uses uk code quickconfig t the that for
---
