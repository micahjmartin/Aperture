---
name: lexfo/rpc2socks
link: https://github.com/lexfo/rpc2socks
language: Python
topics:
- smb
- rpc
- post-exploitation
- windows
- socks
- offensive
- redteam
description: Post-exploit tool that enables a SOCKS tunnel via a Windows host using
  an extensible custom RPC proto over SMB through a named pipe.
writeup: ''
readme: an 05 solution big proto if errors specific manually pipes handle conditions
  speed Where 000 vary state depicted status preferably capabilities see files execute
  220 64 high socksbind most chunk them flaws Os can READ able first O bounce Manager
  Known extensible though Visual 869 meet issues help forces give linked paired this
  python3 communicating authentication Embed dropping Because Vista regarding about
  moment deleting significant disconnected ish listens above future day violation
  exclusively also Launch Clause PASSWORD Open established size integrator hardly
  one execution connectivity instance 581 smb3 okay operations buy via Lexfo auth
  related activate High commands compile dozens are handshake directly 18 virtualenv
  executed lead delays password pip close e around behavior Server dependency long
  code Create q example 32 still caller In ConRelease LexfoSecurite compiled asynchronous
  and statically global locally custom architectures quit its connections querying
  Client through regardless 13 using process created com sole terminating practicable
  Although socket credentials g resolution maintainers Us the limit _ a line Use Installation
  build think root respectively update transactions Unless www worst 0 own itself
  supported because frozen lib too purpose License 591 Notes 2019 exe Mansion clone
  D of order working main part that reasonable 269 found 12 directory Studio once
  internal ends under SOCKS5 instances will well should UNSINSTALL The from usage
  case BLOCKCHAINDOMAIN terms by PENDING sending environment We no layers 8889 uninst
  running correctly firefox response PROTO timeouts creating author vs 4 1 mechanism
  trouble simultaneously An Install timeout connect twitter required application blocking
  complex been co throttling which need 584 Carlos skyrocketing number installed inst
  check project Batch potential github venv noting org occurred SvcRelease least later
  DNS rate note 02 source ideally way avoid filesystem since maximum added workaround
  due Twitter connected closed section See be Leave except winlfo32 access or script
  RPC link classes i wiki more copied R run at Compile py canceling Unix protocol
  all Executable interactions work so both with 234 use reliable 088 en Run following
  must Contact leaving localhost 14 bridge 114 127 configurations result out cause
  tested called 04 logic windows GitHub then While aware between With limited changing
  in Python3 scenario methods impacket_ TCP any across If extracting like remotely
  commando projects but Note license SecureAuthCorp Properties TYPE fail described
  takes git listen easily Side welcome 2008 now here feature packets compiler Requirements
  SMB3 WMI advised YOUR return workarounds same https try 594 wikipedia That port
  embedded keep addendum proxy up lexfo seem crash released 6 do compatible ADMIN
  read additional x64 native consists 3 install No style development identified includes
  is 219 v5 Making gets Typical logical 5MiB version console not shameless for implements
  side 445 does tools A strongly Server_Message_Block deal Solution transaction recvSMB
  acts One sides on 2 _impacket themselves download want It sockets scripts executables
  made st SMB_ such even we 15 updated enabled allows 211 m makes host prompt 367
  step spread trying many CLI require Steps cli could re bigger Password communicate
  to drop your respective BSD named bin Service pair proven low bit you default create
  This waiting than serve during I single issue safier other server client thus requires
  feedback implementations Commit stop listening impacket afford built pipe have SMBConnection
  stay 3988 regular virtual dealing safe Fetch SMB class sln remove package unix file
  clients 01 s follow smbconnection level successful LICENSE flow 17 service embexe_data
  needed request stuff embedsrv has implemented user already huge configure As unnoticed
  mode giving More department fr arch instead threads supporting place 10 100 end
  x86 buffered they handled executable nor remote few worth Maniac svc connection
  872 Python lower patch checking MiB channel may much forcefully only time 56 transport
  however bloc memory target Check seconds command some At located Double FYI cumbersome
  newly method paradigm actual automatic copy SOCKS until convenience option Compiler
  virtually integration extra connects unsubtle accessible rpc2socks where 368 being
  write as Internets two multiple data exists it depending v _SMB Thus requested each
  integrated subsequent For beer over interactive setup reconnect C dedicated Windows
  used tunnel below reasons
---
